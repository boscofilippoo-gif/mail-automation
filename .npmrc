# Con NODE_ENV=production npm ometterebbe i devDependencies, ma il build ha
# bisogno di typescript/vite/@types: forziamo l'installazione completa.
# (Il runtime non ne risente: è solo spazio in più nel build.)
include=dev
